# winapi-resource-exhaustion-legacy
A first-semester Computer Science project written in C. It explores WinAPI functions to simulate system resource exhaustion (RAM stress test) by spawning processes until a critical threshold is met.
